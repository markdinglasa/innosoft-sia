## Feature Pattern

# Structure
/features
    /feature-name
        /api
            react-queries
            query-keys
        /components
            cards
            list
            forms
        /hooks
            filter-hooks
        /store
        /types
        /validators

