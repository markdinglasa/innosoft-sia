/*export const collectionMethods = (dateStart: string, dateEnd: string) => {
  return `

    `
}
export const saleItems = (dateStart: string, dateEnd: string) => {
  return `

    `
}
export const salesDetails = (dateStart: string, dateEnd: string) => {
  return ``
}
*/
